import { CoordinatesOffset } from "./CoordinatesOffseter/CoordinatesOffset.js";

export class CoordinatesOffseter {

    constructor(x, y) {
        
        this.CoordinatesOffset = new CoordinatesOffset();
    }
}